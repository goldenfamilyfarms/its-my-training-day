# Same Tree

## Directory
- Data structure: `trees`
- Technique: `tree-dfs`

## Approach
Compare nodes recursively in preorder.

## Data Structure Deep Dive
- Recursive traversal collects or aggregates subtree data.
- Postorder is ideal for combine/return patterns.
- Null checks guard recursion base cases.

## Complexity
Time O(n) | Space O(h)

## Solution References
- Python: `solution.py` → `same_tree`
- C++: `solution.cpp` → `sameTree`
- TypeScript: `solution.ts` → `sameTree`
