# Detect Cycle in Linked List

## Directory
- Data structure: `linked-list`
- Technique: `linked-list`

## Approach
Floyd's fast/slow pointers detect a loop.

## Data Structure Deep Dive
- Pointer updates are O(1) once nodes are located.
- Dummy nodes simplify head edge cases.
- Fast/slow pointers avoid extra passes.

## Complexity
Time O(n) | Space O(1)

## Solution References
- Python: `solution.py` → `detect_cycle`
- C++: `solution.cpp` → `detectCycle`
- TypeScript: `solution.ts` → `detectCycle`
